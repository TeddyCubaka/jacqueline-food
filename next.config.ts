import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com", "photos.app.goo.gl", 'www.bien-manger-selon-les-saisons.com'],
  },
};

export default nextConfig;
